import React, { useState } from "react";
import SideMenu from "../components/SideMenu";

const Dashboard = () => {
  return (
    <div>
      <SideMenu />
    </div>
  );
};

export default Dashboard;
